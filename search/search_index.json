{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Home","text":""},{"location":"index.html#hostlist","title":"Hostlist","text":"<p>A clean, modern, and fully\u2011typed Python library for parsing and manipulating Slurm\u2011style hostlists.</p>"},{"location":"index.html#features","title":"Features","text":"<ul> <li>Parses both plain and compressed Slurm\u2011style hostlists.</li> <li>Expands them to a sorted list of hostnames.</li> <li>Offers set\u2011like operations (<code>diff</code>, <code>intersect</code>, <code>union</code>, <code>xor</code>).</li> <li>Provides handy helpers (<code>nth</code>, <code>find</code>, <code>count</code>, <code>remove</code>, <code>delimiter</code>, <code>size</code>, <code>exclude</code>, <code>quiet</code>).</li> <li>Template Formatting: Generate a list of strings (e.g., shell commands) for all hosts.</li> <li>CLI Tools: Expand and compact hostlists directly from the shell.</li> <li>Can return either an expanded comma\u2011separated string or a compressed representation.</li> </ul>"},{"location":"index.html#installation","title":"Installation","text":"<pre><code>pip install .\n</code></pre>"},{"location":"index.html#usage","title":"Usage","text":""},{"location":"index.html#python-api","title":"Python API","text":"<pre><code>from hostlist import Hostlist\n\n# create a Hostlist from a compressed string\nhl = Hostlist.expand(\"node[01-03,07],gpu[1-2]\")\n\nprint(\"Expanded:\", hl.compact(compress=False))\n# \u2192 Expanded: gpu1,gpu2,node01,node02,node03,node07\n\nprint(\"Compressed:\", hl.compact())\n# \u2192 Compressed: gpu[1-2],node[01-03,07]\n\n# set\u2011operations\nother = Hostlist.expand(\"node[02-04],gpu[2]\")\nprint(\"Diff:\", hl.diff(other).compact())\n# \u2192 Diff: gpu[1],node[01,03,07]\n\n# Template formatting\ncmds = hl.format(\"ssh {host} 'hostname'\")\n# \u2192 [\"ssh gpu1 'hostname'\", \"ssh gpu2 'hostname'\", ...]\n</code></pre>"},{"location":"index.html#command-line-interface","title":"Command Line Interface","text":"<p>After installing the package, you can use the following commands:</p> <p>Expand a compressed list: <pre><code>hostlist-expand \"node[01-03],gpu[1-2]\"\n# \u2192 gpu1,gpu2,node01,node02,node03,node07\n</code></pre></p> <p>Compact an expanded list: <pre><code>hostlist-compact \"node1,node2,node3\"\n# \u2192 node[1-3]\n</code></pre></p> <p>General utility: <pre><code>hostlist expand \"node[01-03]\"\n# \u2192 node01,node02,node03\n</code></pre></p>"},{"location":"api.html","title":"API Reference","text":""},{"location":"api.html#api-reference","title":"API Reference","text":""},{"location":"api.html#hostlist-attributes","title":"Attributes","text":""},{"location":"api.html#hostlist.__version__","title":"__version__  <code>module-attribute</code>","text":"<pre><code>__version__ = _read_version()\n</code></pre>"},{"location":"api.html#hostlist-classes","title":"Classes","text":""},{"location":"api.html#hostlist.Hostlist","title":"Hostlist  <code>dataclass</code>","text":"<p>Slurm\u2011style hostlist handling.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>@dataclass(frozen=True)\nclass Hostlist:\n    \"\"\"Slurm\u2011style hostlist handling.\"\"\"\n    hosts: tuple[str, ...]\n\n    # ------------------------------------------------------------------\n    # Construction helpers\n    # ------------------------------------------------------------------\n    @classmethod\n    def expand(cls, spec: str) -&gt; \"Hostlist\":\n        \"\"\"Parse a possibly\u2011compressed hostlist string.\"\"\"\n        if not spec:\n            return cls(())\n\n        # split on commas that are **outside** brackets\n        parts, buf, depth = [], \"\", 0\n        for ch in spec:\n            if ch == \"[\":\n                depth += 1\n            elif ch == \"]\":\n                depth -= 1\n                if depth &lt; 0:\n                    raise ValueError(\"Unexpected closing bracket ']' in hostlist\")\n            if ch == \",\" and depth == 0:\n                parts.append(buf)\n                buf = \"\"\n            else:\n                buf += ch\n\n        if depth != 0:\n            raise ValueError(\"Unclosed bracket '[' in hostlist\")\n\n        if buf:\n            parts.append(buf)\n\n        expanded: list[str] = []\n        for token in parts:\n            token = token.strip()\n            if not token:\n                continue\n            m = re.fullmatch(\n                r\"(?P&lt;prefix&gt;[\\w-]+)\\[(?P&lt;body&gt;[^\\]]*)\\](?P&lt;suffix&gt;.*)\", token\n            )\n            if not m:               # plain host (no brackets)\n                expanded.append(token)\n                continue\n\n            prefix, body, suffix = m.group(\"prefix\"), m.group(\"body\"), m.group(\"suffix\")\n\n            # Determine zero\u2011padding width from the first numeric token\n            pad = 0\n            for sub in body.split(\",\"):\n                sub = sub.strip()\n                if not sub: continue\n                start = sub.split(\"-\", 1)[0] if \"-\" in sub else sub\n                if start.lstrip(\"0\") != start:          # leading zeros present\n                    pad = max(pad, len(start))\n\n            try:\n                for num in _expand_range(body):\n                    expanded.append(f\"{prefix}{str(num).zfill(pad)}{suffix}\")\n            except ValueError as e:\n                raise ValueError(f\"Invalid range in bracket for {token!r}: {e}\")\n\n        # Normalise ordering (numeric order per prefix/suffix)\n        expanded.sort(key=_sort_key)\n        return cls(tuple(expanded))\n\n    @classmethod\n    def from_list(cls, hosts: Iterable[str]) -&gt; \"Hostlist\":\n        \"\"\"Create a Hostlist from an already\u2011expanded iterable.\"\"\"\n        # Convert to set to remove duplicates, then sort and tuple\n        sorted_hosts = sorted(set(hosts), key=_sort_key)\n        return cls(tuple(sorted_hosts))\n\n\n    # ------------------------------------------------------------------\n    # Formatting\n    # ------------------------------------------------------------------\n    def compact(self, compress: bool = True) -&gt; str:\n        \"\"\"Return a comma\u2011separated hostlist; optionally compress it.\"\"\"\n        if not self.hosts:\n            return \"\"\n        if not compress:\n            return \",\".join(self.hosts)\n\n        groups = _group_by_prefix(self.hosts)\n        parts = []\n        for (prefix, suffix) in sorted(groups.keys()):\n            nums = groups[(prefix, suffix)]\n\n            numeric = sorted([n for n in nums if n is not None])\n\n            if not numeric:\n                # Just a literal host\n                parts.append(f\"{prefix}{suffix}\")\n                continue\n\n            # Infer padding width from the first numeric occurrence of this prefix/suffix\n            first_host = next(h for h in self.hosts if h.startswith(prefix) and h.endswith(suffix) and any(c.isdigit() for c in h))\n            num_match = re.search(rf\"{re.escape(prefix)}(?P&lt;num&gt;\\d+){re.escape(suffix)}\", first_host)\n            pad = len(num_match.group(\"num\")) if num_match else 0\n\n            parts.append(f\"{prefix}[{_compress_numbers(numeric, pad)}]{suffix}\")\n\n        return \",\".join(parts)\n\n    # ------------------------------------------------------------------\n    # Set\u2011like operations (return new Hostlist instances)\n    # ------------------------------------------------------------------\n    def _as_set(self) -&gt; set[str]:\n        return set(self.hosts)\n\n    def diff(self, *others: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        \"\"\"self \u2013 union(others)\"\"\"\n        base = self._as_set()\n        for o in others:\n            base.difference_update(_coerce(o)._as_set())\n        return Hostlist.from_list(sorted(base, key=lambda h: _parse_node(h)))\n\n    def intersect(self, *others: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        base = self._as_set()\n        for o in others:\n            base.intersection_update(_coerce(o)._as_set())\n        return Hostlist.from_list(sorted(base, key=lambda h: _parse_node(h)))\n\n    def union(self, *others: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        base = self._as_set()\n        for o in others:\n            base.update(_coerce(o)._as_set())\n        return Hostlist.from_list(sorted(base, key=lambda h: _parse_node(h)))\n\n    def xor(self, *others: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        base = self._as_set()\n        for o in others:\n            base.symmetric_difference_update(_coerce(o)._as_set())\n        return Hostlist.from_list(sorted(base, key=lambda h: _parse_node(h)))\n\n    # ------------------------------------------------------------------\n    # Collection API (Magic Methods)\n    # ------------------------------------------------------------------\n    def __len__(self) -&gt; int:\n        return len(self.hosts)\n\n    def __iter__(self):\n        return iter(self.hosts)\n\n    def __contains__(self, item: object) -&gt; bool:\n        return item in self.hosts\n\n    def __getitem__(self, index: Union[int, slice]) -&gt; Union[str, \"Hostlist\"]:\n        \"\"\"Supports 0-based indexing and slicing.\"\"\"\n        if isinstance(index, slice):\n            return Hostlist.from_list(self.hosts[index])\n        return self.hosts[index]\n\n    def __repr__(self) -&gt; str:\n        return f\"Hostlist({self.hosts!r})\"\n\n    # ------------------------------------------------------------------\n    # Set Operators\n    # ------------------------------------------------------------------\n    def __and__(self, other: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        return self.intersect(other)\n\n    def __or__(self, other: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        return self.union(other)\n\n    def __sub__(self, other: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        return self.diff(other)\n\n    def __xor__(self, other: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        return self.xor(other)\n\n    # ------------------------------------------------------------------\n    # Misc helpers\n    # ------------------------------------------------------------------\n    def nth(self, n: int) -&gt; str:\n        \"\"\"Return the n\u2011th host (1\u2011based).\"\"\"\n        if n &lt; 1 or n &gt; len(self):\n            raise ValueError(\"index out of range\")\n        return self[n - 1]\n\n    def find(self, host: str) -&gt; int:\n        \"\"\"Return 1\u2011based position of *host*.\"\"\"\n        try:\n            return self.hosts.index(host) + 1\n        except ValueError:\n            raise ValueError(\"host not found\") from None\n\n    def count(self) -&gt; int:\n        \"\"\"Backward compatible with __len__.\"\"\"\n        return len(self)\n\n    def remove(self, host: str) -&gt; \"Hostlist\":\n        \"\"\"Return a new Hostlist without *host*.\"\"\"\n        if host not in self:\n            raise ValueError(\"host not found\")\n        new = [h for h in self.hosts if h != host]\n        return Hostlist.from_list(new)\n\n    def delimiter(self, d: str) -&gt; str:\n        \"\"\"Join the list with a custom delimiter (no compression).\"\"\"\n        return d.join(self.hosts)\n\n    def size(self, N: int) -&gt; \"Hostlist\":\n        \"\"\"Return at most N hosts (negative \u2192 last N).\"\"\"\n        return Hostlist.from_list(self.hosts[:N] if N &gt;= 0 else self.hosts[N:])\n\n    def exclude(self, *hosts: str) -&gt; \"Hostlist\":\n        \"\"\"Return a Hostlist without the supplied hosts.\"\"\"\n        remaining = [h for h in self.hosts if h not in hosts]\n        if not remaining:\n            raise ValueError(\"resulting hostlist empty\")\n        return Hostlist.from_list(remaining)\n\n    def quiet(self) -&gt; str:\n        \"\"\"Plain comma\u2011separated list; raise if empty.\"\"\"\n        if not self.hosts:\n            raise ValueError(\"hostlist empty\")\n        return \",\".join(self.hosts)\n\n    def format(self, template: str) -&gt; list[str]:\n        \"\"\"\n        Return a list of formatted strings using the provided template.\n        The template should contain a {host} placeholder.\n        \"\"\"\n        return [template.format(host=h) for h in self.hosts]\n</code></pre>"},{"location":"api.html#hostlist.Hostlist-functions","title":"Methods:","text":""},{"location":"api.html#hostlist.Hostlist.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(index)\n</code></pre> <p>Supports 0-based indexing and slicing.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def __getitem__(self, index: Union[int, slice]) -&gt; Union[str, \"Hostlist\"]:\n    \"\"\"Supports 0-based indexing and slicing.\"\"\"\n    if isinstance(index, slice):\n        return Hostlist.from_list(self.hosts[index])\n    return self.hosts[index]\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.compact","title":"compact","text":"<pre><code>compact(compress=True)\n</code></pre> <p>Return a comma\u2011separated hostlist; optionally compress it.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def compact(self, compress: bool = True) -&gt; str:\n    \"\"\"Return a comma\u2011separated hostlist; optionally compress it.\"\"\"\n    if not self.hosts:\n        return \"\"\n    if not compress:\n        return \",\".join(self.hosts)\n\n    groups = _group_by_prefix(self.hosts)\n    parts = []\n    for (prefix, suffix) in sorted(groups.keys()):\n        nums = groups[(prefix, suffix)]\n\n        numeric = sorted([n for n in nums if n is not None])\n\n        if not numeric:\n            # Just a literal host\n            parts.append(f\"{prefix}{suffix}\")\n            continue\n\n        # Infer padding width from the first numeric occurrence of this prefix/suffix\n        first_host = next(h for h in self.hosts if h.startswith(prefix) and h.endswith(suffix) and any(c.isdigit() for c in h))\n        num_match = re.search(rf\"{re.escape(prefix)}(?P&lt;num&gt;\\d+){re.escape(suffix)}\", first_host)\n        pad = len(num_match.group(\"num\")) if num_match else 0\n\n        parts.append(f\"{prefix}[{_compress_numbers(numeric, pad)}]{suffix}\")\n\n    return \",\".join(parts)\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.count","title":"count","text":"<pre><code>count()\n</code></pre> <p>Backward compatible with len.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def count(self) -&gt; int:\n    \"\"\"Backward compatible with __len__.\"\"\"\n    return len(self)\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.delimiter","title":"delimiter","text":"<pre><code>delimiter(d)\n</code></pre> <p>Join the list with a custom delimiter (no compression).</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def delimiter(self, d: str) -&gt; str:\n    \"\"\"Join the list with a custom delimiter (no compression).\"\"\"\n    return d.join(self.hosts)\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.diff","title":"diff","text":"<pre><code>diff(*others)\n</code></pre> <p>self \u2013 union(others)</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def diff(self, *others: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n    \"\"\"self \u2013 union(others)\"\"\"\n    base = self._as_set()\n    for o in others:\n        base.difference_update(_coerce(o)._as_set())\n    return Hostlist.from_list(sorted(base, key=lambda h: _parse_node(h)))\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.exclude","title":"exclude","text":"<pre><code>exclude(*hosts)\n</code></pre> <p>Return a Hostlist without the supplied hosts.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def exclude(self, *hosts: str) -&gt; \"Hostlist\":\n    \"\"\"Return a Hostlist without the supplied hosts.\"\"\"\n    remaining = [h for h in self.hosts if h not in hosts]\n    if not remaining:\n        raise ValueError(\"resulting hostlist empty\")\n    return Hostlist.from_list(remaining)\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.expand","title":"expand  <code>classmethod</code>","text":"<pre><code>expand(spec)\n</code></pre> <p>Parse a possibly\u2011compressed hostlist string.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>@classmethod\ndef expand(cls, spec: str) -&gt; \"Hostlist\":\n    \"\"\"Parse a possibly\u2011compressed hostlist string.\"\"\"\n    if not spec:\n        return cls(())\n\n    # split on commas that are **outside** brackets\n    parts, buf, depth = [], \"\", 0\n    for ch in spec:\n        if ch == \"[\":\n            depth += 1\n        elif ch == \"]\":\n            depth -= 1\n            if depth &lt; 0:\n                raise ValueError(\"Unexpected closing bracket ']' in hostlist\")\n        if ch == \",\" and depth == 0:\n            parts.append(buf)\n            buf = \"\"\n        else:\n            buf += ch\n\n    if depth != 0:\n        raise ValueError(\"Unclosed bracket '[' in hostlist\")\n\n    if buf:\n        parts.append(buf)\n\n    expanded: list[str] = []\n    for token in parts:\n        token = token.strip()\n        if not token:\n            continue\n        m = re.fullmatch(\n            r\"(?P&lt;prefix&gt;[\\w-]+)\\[(?P&lt;body&gt;[^\\]]*)\\](?P&lt;suffix&gt;.*)\", token\n        )\n        if not m:               # plain host (no brackets)\n            expanded.append(token)\n            continue\n\n        prefix, body, suffix = m.group(\"prefix\"), m.group(\"body\"), m.group(\"suffix\")\n\n        # Determine zero\u2011padding width from the first numeric token\n        pad = 0\n        for sub in body.split(\",\"):\n            sub = sub.strip()\n            if not sub: continue\n            start = sub.split(\"-\", 1)[0] if \"-\" in sub else sub\n            if start.lstrip(\"0\") != start:          # leading zeros present\n                pad = max(pad, len(start))\n\n        try:\n            for num in _expand_range(body):\n                expanded.append(f\"{prefix}{str(num).zfill(pad)}{suffix}\")\n        except ValueError as e:\n            raise ValueError(f\"Invalid range in bracket for {token!r}: {e}\")\n\n    # Normalise ordering (numeric order per prefix/suffix)\n    expanded.sort(key=_sort_key)\n    return cls(tuple(expanded))\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.find","title":"find","text":"<pre><code>find(host)\n</code></pre> <p>Return 1\u2011based position of host.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def find(self, host: str) -&gt; int:\n    \"\"\"Return 1\u2011based position of *host*.\"\"\"\n    try:\n        return self.hosts.index(host) + 1\n    except ValueError:\n        raise ValueError(\"host not found\") from None\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.format","title":"format","text":"<pre><code>format(template)\n</code></pre> <p>Return a list of formatted strings using the provided template. The template should contain a {host} placeholder.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def format(self, template: str) -&gt; list[str]:\n    \"\"\"\n    Return a list of formatted strings using the provided template.\n    The template should contain a {host} placeholder.\n    \"\"\"\n    return [template.format(host=h) for h in self.hosts]\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.from_list","title":"from_list  <code>classmethod</code>","text":"<pre><code>from_list(hosts)\n</code></pre> <p>Create a Hostlist from an already\u2011expanded iterable.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>@classmethod\ndef from_list(cls, hosts: Iterable[str]) -&gt; \"Hostlist\":\n    \"\"\"Create a Hostlist from an already\u2011expanded iterable.\"\"\"\n    # Convert to set to remove duplicates, then sort and tuple\n    sorted_hosts = sorted(set(hosts), key=_sort_key)\n    return cls(tuple(sorted_hosts))\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.nth","title":"nth","text":"<pre><code>nth(n)\n</code></pre> <p>Return the n\u2011th host (1\u2011based).</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def nth(self, n: int) -&gt; str:\n    \"\"\"Return the n\u2011th host (1\u2011based).\"\"\"\n    if n &lt; 1 or n &gt; len(self):\n        raise ValueError(\"index out of range\")\n    return self[n - 1]\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.quiet","title":"quiet","text":"<pre><code>quiet()\n</code></pre> <p>Plain comma\u2011separated list; raise if empty.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def quiet(self) -&gt; str:\n    \"\"\"Plain comma\u2011separated list; raise if empty.\"\"\"\n    if not self.hosts:\n        raise ValueError(\"hostlist empty\")\n    return \",\".join(self.hosts)\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.remove","title":"remove","text":"<pre><code>remove(host)\n</code></pre> <p>Return a new Hostlist without host.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def remove(self, host: str) -&gt; \"Hostlist\":\n    \"\"\"Return a new Hostlist without *host*.\"\"\"\n    if host not in self:\n        raise ValueError(\"host not found\")\n    new = [h for h in self.hosts if h != host]\n    return Hostlist.from_list(new)\n</code></pre>"},{"location":"api.html#hostlist.Hostlist.size","title":"size","text":"<pre><code>size(N)\n</code></pre> <p>Return at most N hosts (negative \u2192 last N).</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def size(self, N: int) -&gt; \"Hostlist\":\n    \"\"\"Return at most N hosts (negative \u2192 last N).\"\"\"\n    return Hostlist.from_list(self.hosts[:N] if N &gt;= 0 else self.hosts[N:])\n</code></pre>"},{"location":"api.html#hostlist-functions","title":"Functions:","text":""},{"location":"api.html#hostlist._read_version","title":"_read_version","text":"<pre><code>_read_version()\n</code></pre> Source code in <code>src/hostlist/__init__.py</code> <pre><code>def _read_version():\n    try:\n        # The VERSION file is in the project root, 2 levels up from this file\n        version_file = os.path.join(os.path.dirname(__file__), '..', '..', 'VERSION')\n        with open(version_file, 'r') as f:\n            return f.read().strip()\n    except FileNotFoundError:\n        return \"0.0.0\"\n</code></pre>"},{"location":"api.html#hostlist-modules","title":"Modules","text":""},{"location":"api.html#hostlist.cli","title":"cli","text":""},{"location":"api.html#hostlist.cli-classes","title":"Classes","text":""},{"location":"api.html#hostlist.core","title":"core","text":""},{"location":"api.html#hostlist.core-classes","title":"Classes","text":""},{"location":"api.html#hostlist.core.Hostlist","title":"Hostlist  <code>dataclass</code>","text":"<p>Slurm\u2011style hostlist handling.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>@dataclass(frozen=True)\nclass Hostlist:\n    \"\"\"Slurm\u2011style hostlist handling.\"\"\"\n    hosts: tuple[str, ...]\n\n    # ------------------------------------------------------------------\n    # Construction helpers\n    # ------------------------------------------------------------------\n    @classmethod\n    def expand(cls, spec: str) -&gt; \"Hostlist\":\n        \"\"\"Parse a possibly\u2011compressed hostlist string.\"\"\"\n        if not spec:\n            return cls(())\n\n        # split on commas that are **outside** brackets\n        parts, buf, depth = [], \"\", 0\n        for ch in spec:\n            if ch == \"[\":\n                depth += 1\n            elif ch == \"]\":\n                depth -= 1\n                if depth &lt; 0:\n                    raise ValueError(\"Unexpected closing bracket ']' in hostlist\")\n            if ch == \",\" and depth == 0:\n                parts.append(buf)\n                buf = \"\"\n            else:\n                buf += ch\n\n        if depth != 0:\n            raise ValueError(\"Unclosed bracket '[' in hostlist\")\n\n        if buf:\n            parts.append(buf)\n\n        expanded: list[str] = []\n        for token in parts:\n            token = token.strip()\n            if not token:\n                continue\n            m = re.fullmatch(\n                r\"(?P&lt;prefix&gt;[\\w-]+)\\[(?P&lt;body&gt;[^\\]]*)\\](?P&lt;suffix&gt;.*)\", token\n            )\n            if not m:               # plain host (no brackets)\n                expanded.append(token)\n                continue\n\n            prefix, body, suffix = m.group(\"prefix\"), m.group(\"body\"), m.group(\"suffix\")\n\n            # Determine zero\u2011padding width from the first numeric token\n            pad = 0\n            for sub in body.split(\",\"):\n                sub = sub.strip()\n                if not sub: continue\n                start = sub.split(\"-\", 1)[0] if \"-\" in sub else sub\n                if start.lstrip(\"0\") != start:          # leading zeros present\n                    pad = max(pad, len(start))\n\n            try:\n                for num in _expand_range(body):\n                    expanded.append(f\"{prefix}{str(num).zfill(pad)}{suffix}\")\n            except ValueError as e:\n                raise ValueError(f\"Invalid range in bracket for {token!r}: {e}\")\n\n        # Normalise ordering (numeric order per prefix/suffix)\n        expanded.sort(key=_sort_key)\n        return cls(tuple(expanded))\n\n    @classmethod\n    def from_list(cls, hosts: Iterable[str]) -&gt; \"Hostlist\":\n        \"\"\"Create a Hostlist from an already\u2011expanded iterable.\"\"\"\n        # Convert to set to remove duplicates, then sort and tuple\n        sorted_hosts = sorted(set(hosts), key=_sort_key)\n        return cls(tuple(sorted_hosts))\n\n\n    # ------------------------------------------------------------------\n    # Formatting\n    # ------------------------------------------------------------------\n    def compact(self, compress: bool = True) -&gt; str:\n        \"\"\"Return a comma\u2011separated hostlist; optionally compress it.\"\"\"\n        if not self.hosts:\n            return \"\"\n        if not compress:\n            return \",\".join(self.hosts)\n\n        groups = _group_by_prefix(self.hosts)\n        parts = []\n        for (prefix, suffix) in sorted(groups.keys()):\n            nums = groups[(prefix, suffix)]\n\n            numeric = sorted([n for n in nums if n is not None])\n\n            if not numeric:\n                # Just a literal host\n                parts.append(f\"{prefix}{suffix}\")\n                continue\n\n            # Infer padding width from the first numeric occurrence of this prefix/suffix\n            first_host = next(h for h in self.hosts if h.startswith(prefix) and h.endswith(suffix) and any(c.isdigit() for c in h))\n            num_match = re.search(rf\"{re.escape(prefix)}(?P&lt;num&gt;\\d+){re.escape(suffix)}\", first_host)\n            pad = len(num_match.group(\"num\")) if num_match else 0\n\n            parts.append(f\"{prefix}[{_compress_numbers(numeric, pad)}]{suffix}\")\n\n        return \",\".join(parts)\n\n    # ------------------------------------------------------------------\n    # Set\u2011like operations (return new Hostlist instances)\n    # ------------------------------------------------------------------\n    def _as_set(self) -&gt; set[str]:\n        return set(self.hosts)\n\n    def diff(self, *others: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        \"\"\"self \u2013 union(others)\"\"\"\n        base = self._as_set()\n        for o in others:\n            base.difference_update(_coerce(o)._as_set())\n        return Hostlist.from_list(sorted(base, key=lambda h: _parse_node(h)))\n\n    def intersect(self, *others: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        base = self._as_set()\n        for o in others:\n            base.intersection_update(_coerce(o)._as_set())\n        return Hostlist.from_list(sorted(base, key=lambda h: _parse_node(h)))\n\n    def union(self, *others: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        base = self._as_set()\n        for o in others:\n            base.update(_coerce(o)._as_set())\n        return Hostlist.from_list(sorted(base, key=lambda h: _parse_node(h)))\n\n    def xor(self, *others: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        base = self._as_set()\n        for o in others:\n            base.symmetric_difference_update(_coerce(o)._as_set())\n        return Hostlist.from_list(sorted(base, key=lambda h: _parse_node(h)))\n\n    # ------------------------------------------------------------------\n    # Collection API (Magic Methods)\n    # ------------------------------------------------------------------\n    def __len__(self) -&gt; int:\n        return len(self.hosts)\n\n    def __iter__(self):\n        return iter(self.hosts)\n\n    def __contains__(self, item: object) -&gt; bool:\n        return item in self.hosts\n\n    def __getitem__(self, index: Union[int, slice]) -&gt; Union[str, \"Hostlist\"]:\n        \"\"\"Supports 0-based indexing and slicing.\"\"\"\n        if isinstance(index, slice):\n            return Hostlist.from_list(self.hosts[index])\n        return self.hosts[index]\n\n    def __repr__(self) -&gt; str:\n        return f\"Hostlist({self.hosts!r})\"\n\n    # ------------------------------------------------------------------\n    # Set Operators\n    # ------------------------------------------------------------------\n    def __and__(self, other: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        return self.intersect(other)\n\n    def __or__(self, other: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        return self.union(other)\n\n    def __sub__(self, other: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        return self.diff(other)\n\n    def __xor__(self, other: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n        return self.xor(other)\n\n    # ------------------------------------------------------------------\n    # Misc helpers\n    # ------------------------------------------------------------------\n    def nth(self, n: int) -&gt; str:\n        \"\"\"Return the n\u2011th host (1\u2011based).\"\"\"\n        if n &lt; 1 or n &gt; len(self):\n            raise ValueError(\"index out of range\")\n        return self[n - 1]\n\n    def find(self, host: str) -&gt; int:\n        \"\"\"Return 1\u2011based position of *host*.\"\"\"\n        try:\n            return self.hosts.index(host) + 1\n        except ValueError:\n            raise ValueError(\"host not found\") from None\n\n    def count(self) -&gt; int:\n        \"\"\"Backward compatible with __len__.\"\"\"\n        return len(self)\n\n    def remove(self, host: str) -&gt; \"Hostlist\":\n        \"\"\"Return a new Hostlist without *host*.\"\"\"\n        if host not in self:\n            raise ValueError(\"host not found\")\n        new = [h for h in self.hosts if h != host]\n        return Hostlist.from_list(new)\n\n    def delimiter(self, d: str) -&gt; str:\n        \"\"\"Join the list with a custom delimiter (no compression).\"\"\"\n        return d.join(self.hosts)\n\n    def size(self, N: int) -&gt; \"Hostlist\":\n        \"\"\"Return at most N hosts (negative \u2192 last N).\"\"\"\n        return Hostlist.from_list(self.hosts[:N] if N &gt;= 0 else self.hosts[N:])\n\n    def exclude(self, *hosts: str) -&gt; \"Hostlist\":\n        \"\"\"Return a Hostlist without the supplied hosts.\"\"\"\n        remaining = [h for h in self.hosts if h not in hosts]\n        if not remaining:\n            raise ValueError(\"resulting hostlist empty\")\n        return Hostlist.from_list(remaining)\n\n    def quiet(self) -&gt; str:\n        \"\"\"Plain comma\u2011separated list; raise if empty.\"\"\"\n        if not self.hosts:\n            raise ValueError(\"hostlist empty\")\n        return \",\".join(self.hosts)\n\n    def format(self, template: str) -&gt; list[str]:\n        \"\"\"\n        Return a list of formatted strings using the provided template.\n        The template should contain a {host} placeholder.\n        \"\"\"\n        return [template.format(host=h) for h in self.hosts]\n</code></pre>"},{"location":"api.html#hostlist.core.Hostlist-functions","title":"Methods:","text":"__getitem__ <pre><code>__getitem__(index)\n</code></pre> <p>Supports 0-based indexing and slicing.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def __getitem__(self, index: Union[int, slice]) -&gt; Union[str, \"Hostlist\"]:\n    \"\"\"Supports 0-based indexing and slicing.\"\"\"\n    if isinstance(index, slice):\n        return Hostlist.from_list(self.hosts[index])\n    return self.hosts[index]\n</code></pre> compact <pre><code>compact(compress=True)\n</code></pre> <p>Return a comma\u2011separated hostlist; optionally compress it.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def compact(self, compress: bool = True) -&gt; str:\n    \"\"\"Return a comma\u2011separated hostlist; optionally compress it.\"\"\"\n    if not self.hosts:\n        return \"\"\n    if not compress:\n        return \",\".join(self.hosts)\n\n    groups = _group_by_prefix(self.hosts)\n    parts = []\n    for (prefix, suffix) in sorted(groups.keys()):\n        nums = groups[(prefix, suffix)]\n\n        numeric = sorted([n for n in nums if n is not None])\n\n        if not numeric:\n            # Just a literal host\n            parts.append(f\"{prefix}{suffix}\")\n            continue\n\n        # Infer padding width from the first numeric occurrence of this prefix/suffix\n        first_host = next(h for h in self.hosts if h.startswith(prefix) and h.endswith(suffix) and any(c.isdigit() for c in h))\n        num_match = re.search(rf\"{re.escape(prefix)}(?P&lt;num&gt;\\d+){re.escape(suffix)}\", first_host)\n        pad = len(num_match.group(\"num\")) if num_match else 0\n\n        parts.append(f\"{prefix}[{_compress_numbers(numeric, pad)}]{suffix}\")\n\n    return \",\".join(parts)\n</code></pre> count <pre><code>count()\n</code></pre> <p>Backward compatible with len.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def count(self) -&gt; int:\n    \"\"\"Backward compatible with __len__.\"\"\"\n    return len(self)\n</code></pre> delimiter <pre><code>delimiter(d)\n</code></pre> <p>Join the list with a custom delimiter (no compression).</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def delimiter(self, d: str) -&gt; str:\n    \"\"\"Join the list with a custom delimiter (no compression).\"\"\"\n    return d.join(self.hosts)\n</code></pre> diff <pre><code>diff(*others)\n</code></pre> <p>self \u2013 union(others)</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def diff(self, *others: Union[\"Hostlist\", str, list[str]]) -&gt; \"Hostlist\":\n    \"\"\"self \u2013 union(others)\"\"\"\n    base = self._as_set()\n    for o in others:\n        base.difference_update(_coerce(o)._as_set())\n    return Hostlist.from_list(sorted(base, key=lambda h: _parse_node(h)))\n</code></pre> exclude <pre><code>exclude(*hosts)\n</code></pre> <p>Return a Hostlist without the supplied hosts.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def exclude(self, *hosts: str) -&gt; \"Hostlist\":\n    \"\"\"Return a Hostlist without the supplied hosts.\"\"\"\n    remaining = [h for h in self.hosts if h not in hosts]\n    if not remaining:\n        raise ValueError(\"resulting hostlist empty\")\n    return Hostlist.from_list(remaining)\n</code></pre> expand <code>classmethod</code> <pre><code>expand(spec)\n</code></pre> <p>Parse a possibly\u2011compressed hostlist string.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>@classmethod\ndef expand(cls, spec: str) -&gt; \"Hostlist\":\n    \"\"\"Parse a possibly\u2011compressed hostlist string.\"\"\"\n    if not spec:\n        return cls(())\n\n    # split on commas that are **outside** brackets\n    parts, buf, depth = [], \"\", 0\n    for ch in spec:\n        if ch == \"[\":\n            depth += 1\n        elif ch == \"]\":\n            depth -= 1\n            if depth &lt; 0:\n                raise ValueError(\"Unexpected closing bracket ']' in hostlist\")\n        if ch == \",\" and depth == 0:\n            parts.append(buf)\n            buf = \"\"\n        else:\n            buf += ch\n\n    if depth != 0:\n        raise ValueError(\"Unclosed bracket '[' in hostlist\")\n\n    if buf:\n        parts.append(buf)\n\n    expanded: list[str] = []\n    for token in parts:\n        token = token.strip()\n        if not token:\n            continue\n        m = re.fullmatch(\n            r\"(?P&lt;prefix&gt;[\\w-]+)\\[(?P&lt;body&gt;[^\\]]*)\\](?P&lt;suffix&gt;.*)\", token\n        )\n        if not m:               # plain host (no brackets)\n            expanded.append(token)\n            continue\n\n        prefix, body, suffix = m.group(\"prefix\"), m.group(\"body\"), m.group(\"suffix\")\n\n        # Determine zero\u2011padding width from the first numeric token\n        pad = 0\n        for sub in body.split(\",\"):\n            sub = sub.strip()\n            if not sub: continue\n            start = sub.split(\"-\", 1)[0] if \"-\" in sub else sub\n            if start.lstrip(\"0\") != start:          # leading zeros present\n                pad = max(pad, len(start))\n\n        try:\n            for num in _expand_range(body):\n                expanded.append(f\"{prefix}{str(num).zfill(pad)}{suffix}\")\n        except ValueError as e:\n            raise ValueError(f\"Invalid range in bracket for {token!r}: {e}\")\n\n    # Normalise ordering (numeric order per prefix/suffix)\n    expanded.sort(key=_sort_key)\n    return cls(tuple(expanded))\n</code></pre> find <pre><code>find(host)\n</code></pre> <p>Return 1\u2011based position of host.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def find(self, host: str) -&gt; int:\n    \"\"\"Return 1\u2011based position of *host*.\"\"\"\n    try:\n        return self.hosts.index(host) + 1\n    except ValueError:\n        raise ValueError(\"host not found\") from None\n</code></pre> format <pre><code>format(template)\n</code></pre> <p>Return a list of formatted strings using the provided template. The template should contain a {host} placeholder.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def format(self, template: str) -&gt; list[str]:\n    \"\"\"\n    Return a list of formatted strings using the provided template.\n    The template should contain a {host} placeholder.\n    \"\"\"\n    return [template.format(host=h) for h in self.hosts]\n</code></pre> from_list <code>classmethod</code> <pre><code>from_list(hosts)\n</code></pre> <p>Create a Hostlist from an already\u2011expanded iterable.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>@classmethod\ndef from_list(cls, hosts: Iterable[str]) -&gt; \"Hostlist\":\n    \"\"\"Create a Hostlist from an already\u2011expanded iterable.\"\"\"\n    # Convert to set to remove duplicates, then sort and tuple\n    sorted_hosts = sorted(set(hosts), key=_sort_key)\n    return cls(tuple(sorted_hosts))\n</code></pre> nth <pre><code>nth(n)\n</code></pre> <p>Return the n\u2011th host (1\u2011based).</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def nth(self, n: int) -&gt; str:\n    \"\"\"Return the n\u2011th host (1\u2011based).\"\"\"\n    if n &lt; 1 or n &gt; len(self):\n        raise ValueError(\"index out of range\")\n    return self[n - 1]\n</code></pre> quiet <pre><code>quiet()\n</code></pre> <p>Plain comma\u2011separated list; raise if empty.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def quiet(self) -&gt; str:\n    \"\"\"Plain comma\u2011separated list; raise if empty.\"\"\"\n    if not self.hosts:\n        raise ValueError(\"hostlist empty\")\n    return \",\".join(self.hosts)\n</code></pre> remove <pre><code>remove(host)\n</code></pre> <p>Return a new Hostlist without host.</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def remove(self, host: str) -&gt; \"Hostlist\":\n    \"\"\"Return a new Hostlist without *host*.\"\"\"\n    if host not in self:\n        raise ValueError(\"host not found\")\n    new = [h for h in self.hosts if h != host]\n    return Hostlist.from_list(new)\n</code></pre> size <pre><code>size(N)\n</code></pre> <p>Return at most N hosts (negative \u2192 last N).</p> Source code in <code>src/hostlist/core.py</code> <pre><code>def size(self, N: int) -&gt; \"Hostlist\":\n    \"\"\"Return at most N hosts (negative \u2192 last N).\"\"\"\n    return Hostlist.from_list(self.hosts[:N] if N &gt;= 0 else self.hosts[N:])\n</code></pre>"},{"location":"cli.html","title":"CLI Tool","text":""},{"location":"cli.html#cli-tool","title":"CLI Tool","text":"<p>The <code>hostlist</code> command-line tool provides a quick way to expand and compact hostlists from the terminal.</p>"},{"location":"cli.html#expand","title":"Expand","text":"<p>Expand a compressed specification into a list of hosts.</p> <pre><code># Basic expansion\nhostlist expand \"node[01-03,07]\"\n# Output: node01,node02,node03,node07\n\n# Quiet output (comma-separated)\nhostlist expand \"node[1-3]\" --quiet\n# Output: node1,node2,node3\n\n# Formatted output\nhostlist expand \"node[1-3]\" --format \"ssh {host} uptime\"\n# Output:\n# ssh node1 uptime\n# ssh node2 uptime\n# ssh node3 uptime\n</code></pre>"},{"location":"cli.html#compact","title":"Compact","text":"<p>Take a list of hostnames and compress them into Slurm style.</p> <pre><code>hostlist compact \"node1,node2,node3,node7\"\n# Output: node[1-3,7]\n</code></pre>"},{"location":"cli.html#pipeline-support","title":"Pipeline Support","text":"<p>The CLI supports reading from <code>stdin</code>, allowing it to be used in pipelines:</p> <pre><code>echo \"node[1-3]\" | hostlist expand\n</code></pre>"},{"location":"collection_api.html","title":"Collection API","text":""},{"location":"collection_api.html#collection-api","title":"Collection API","text":"<p>A <code>Hostlist</code> object behaves like a read-only sequence of hostnames, providing several useful collection-based methods.</p>"},{"location":"collection_api.html#indexing-and-slicing","title":"Indexing and Slicing","text":"<p>You can access hosts by index or take a slice of the hostlist: <pre><code>hl = Hostlist.expand(\"node[1-10]\")\n\n# Get the first host\nprint(hl[0]) # \"node1\"\n\n# Get a slice (returns a new Hostlist)\nsub_hl = hl[0:3] \nprint(sub_hl.compact()) # \"node[1-3]\"\n</code></pre></p>"},{"location":"collection_api.html#position-and-search","title":"Position and Search","text":"<ul> <li><code>nth(n)</code>: Returns the $n$-th host (1-based indexing).</li> <li><code>find(host)</code>: Returns the 1-based position of a specific host.</li> </ul> <pre><code>hl = Hostlist.expand(\"node[1-10]\")\nprint(hl.nth(2))      # \"node2\"\nprint(hl.find(\"node2\")) # 2\n</code></pre>"},{"location":"collection_api.html#size-and-filtering","title":"Size and Filtering","text":"<ul> <li><code>size(N)</code>: Returns a new <code>Hostlist</code> containing at most $N$ hosts. Use negative numbers to get the last $N$ hosts.</li> <li><code>exclude(*hosts)</code>: Returns a new <code>Hostlist</code> with the specified hosts removed.</li> </ul> <pre><code>hl = Hostlist.expand(\"node[1-10]\")\nprint(hl.size(3).compact())    # \"node[1-3]\"\nprint(hl.size(-2).compact())   # \"node[9-10]\"\nprint(hl.exclude(\"node1\").compact()) # \"node[2-10]\"\n</code></pre>"},{"location":"collection_api.html#iteration-and-length","title":"Iteration and Length","text":"<p><code>Hostlist</code> supports standard Python collection protocols: <pre><code>hl = Hostlist.expand(\"node[1-3]\")\nprint(len(hl)) # 3\nfor host in hl:\n    print(host)\n</code></pre></p>"},{"location":"compression.html","title":"Compression","text":""},{"location":"compression.html#compression","title":"Compression","text":"<p>The <code>compact()</code> method converts a <code>Hostlist</code> object back into a compressed Slurm-style string.</p>"},{"location":"compression.html#basic-compression","title":"Basic Compression","text":"<p>A sequence of numeric hosts is compressed into a range: <pre><code>from hostlist import Hostlist\nhl = Hostlist.from_list([\"node1\", \"node2\", \"node3\"])\nprint(hl.compact()) \n# Output: \"node[1-3]\"\n</code></pre></p>"},{"location":"compression.html#mixed-compression","title":"Mixed Compression","text":"<p>Literal hosts are kept separate, while numeric hosts are grouped and compressed: <pre><code>hl = Hostlist.from_list([\"node1\", \"node2\", \"node3\", \"headnode\"])\nprint(hl.compact())\n# Output: \"headnode,node[1-3]\"\n</code></pre></p>"},{"location":"compression.html#preservation-of-padding","title":"Preservation of Padding","text":"<p>The <code>compact()</code> method preserves the padding found in the hosts: <pre><code>hl = Hostlist.from_list([\"node01\", \"node02\", \"node03\"])\nprint(hl.compact())\n# Output: \"node[01-03]\"\n</code></pre></p>"},{"location":"compression.html#uncompressed-output","title":"Uncompressed Output","text":"<p>If you need the hosts as a simple comma-separated list without ranges, use <code>compress=False</code>: <pre><code>print(hl.compact(compress=False))\n# Output: \"node01,node02,node03\"\n</code></pre></p>"},{"location":"expansion.html","title":"Expansion","text":""},{"location":"expansion.html#expansion","title":"Expansion","text":"<p>The <code>Hostlist.expand()</code> method converts a Slurm-style compressed hostlist string into a <code>Hostlist</code> object containing all individual hostnames.</p>"},{"location":"expansion.html#basic-expansion","title":"Basic Expansion","text":"<p>A simple range is expanded into its constituent hosts: <pre><code>from hostlist import Hostlist\nhl = Hostlist.expand(\"node[01-03]\")\n# Result: [\"node01\", \"node02\", \"node03\"]\n</code></pre></p>"},{"location":"expansion.html#multiple-ranges-and-individuals","title":"Multiple Ranges and Individuals","text":"<p>You can combine multiple ranges and individual hosts in a single specification: <pre><code>hl = Hostlist.expand(\"node[01-03,07],gpu01\")\n# Result: [\"gpu01\", \"node01\", \"node02\", \"node03\", \"node07\"]\n</code></pre></p>"},{"location":"expansion.html#zero-padding","title":"Zero Padding","text":"<p>The library automatically detects the padding width from the specification to preserve leading zeros: <pre><code>hl = Hostlist.expand(\"node[001-002]\")\n# Result: [\"node001\", \"node002\"]\n</code></pre></p>"},{"location":"expansion.html#mixed-hostnames","title":"Mixed Hostnames","text":"<p>Hostlists can contain a mix of numeric ranges and literal hostnames: <pre><code>hl = Hostlist.expand(\"node[1-3],headnode\")\n# Result: [\"headnode\", \"node1\", \"node2\", \"node3\"]\n</code></pre></p>"},{"location":"expansion.html#suffixes","title":"Suffixes","text":"<p>Suffixes (like domain names) are preserved during expansion: <pre><code>hl = Hostlist.expand(\"node[1-3].cluster.local\")\n# Result: [\"node1.cluster.local\", \"node2.cluster.local\", \"node3.cluster.local\"]\n</code></pre></p>"},{"location":"formatting.html","title":"Formatting","text":""},{"location":"formatting.html#formatting","title":"Formatting","text":"<p>The <code>.format()</code> method allows you to generate lists of strings based on a template, which is particularly useful for creating shell commands.</p>"},{"location":"formatting.html#basic-usage","title":"Basic Usage","text":"<p>Provide a template string containing the <code>{host}</code> placeholder: <pre><code>from hostlist import Hostlist\nhl = Hostlist.expand(\"node[1-3]\")\n\ncommands = hl.format(\"ssh {host} 'uptime'\")\n# Result:\n# [\n#   \"ssh node1 'uptime'\",\n#   \"ssh node2 'uptime'\",\n#   \"ssh node3 'uptime'\"\n# ]\n</code></pre></p>"},{"location":"formatting.html#common-use-cases","title":"Common Use Cases","text":"<p>This is ideal for generating scripts or command-line arguments: <pre><code># Generating a list of target nodes for a tool\ntargets = hl.format(\"--node {host}\")\n# [\"--node node1\", \"--node node2\", \"--node node3\"]\n</code></pre></p>"},{"location":"set_operations.html","title":"Set Operations","text":""},{"location":"set_operations.html#set-operations","title":"Set Operations","text":"<p>The <code>Hostlist</code> class supports powerful set operations, allowing you to manipulate groups of hosts using both method calls and Python operators.</p>"},{"location":"set_operations.html#available-operations","title":"Available Operations","text":"Operation Method Operator Description Union <code>.union()</code> <code>\\|</code> All hosts in either hostlist. Intersection <code>.intersect()</code> <code>&amp;</code> Only hosts present in both hostlists. Difference <code>.diff()</code> <code>-</code> Hosts in the first list but not the second. Symmetric Diff <code>.xor()</code> <code>^</code> Hosts in either list, but not both."},{"location":"set_operations.html#examples","title":"Examples","text":"<pre><code>from hostlist import Hostlist\na = Hostlist.expand(\"node[1-5]\")\nb = Hostlist.expand(\"node[4-8]\")\n\n# Union: node[1-8]\nprint((a \\| b).compact())\n\n# Intersection: node[4-5]\nprint((a &amp; b).compact())\n\n# Difference: node[1-3]\nprint((a - b).compact())\n\n# Symmetric Difference: node[1-3,6-8]\nprint((a ^ b).compact())\n</code></pre>"},{"location":"set_operations.html#coercion","title":"Coercion","text":"<p>Set operations automatically coerce strings or lists into <code>Hostlist</code> objects: <pre><code>hl = Hostlist.expand(\"node[1-3]\")\nresult = hl \\| \"node4\" # Result: node[1-4]\n</code></pre></p>"}]}